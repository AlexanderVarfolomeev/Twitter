<Window x:Class="Twitter.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Twitter.WPF.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <x:Static Member="vm:ViewModelLocator.MainViewModel" x:Key="Locator"></x:Static>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="addsf" Command="{Binding LoginCommand, Source={StaticResource Locator}}"></Button>
        <TextBlock Grid.Row="1" Text="{Binding Token, Source={StaticResource Locator}}"></TextBlock>
    </Grid>
</Window>
